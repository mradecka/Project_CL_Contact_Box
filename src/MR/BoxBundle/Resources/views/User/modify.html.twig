{% extends "::base.html.twig" %}

{% block title %}Edycja kontaktu{% endblock %}

{% block body %}
    <div class="col-lg-2">
    </div>
    <div class="col-lg-10">
        <h1>Modyfikowanie kontaktu:</h1>
        {%if info%}
            <h3>{{info}}</h3>  
        {%endif%}
        {%if form%} 
            {{form_start(form, { 'attr': {'class': 'form-horizontale col-md-4'}}) }}

            <div class="form-group">
                <label class="control-label col-sm-6" for="email">Imię:</label>
                <div class="col-sm-6">

                    {{form_widget(form.name)}}
                </div>
            </div>
            <div class="form-group">
                <label class="control-label col-sm-6" for="pwd">Nazwisko:</label>
                <div class="col-sm-6">
                    {{form_widget(form.surname)}}
                </div>
            </div>
            <div class="form-group">
                <label class="control-label col-sm-6" for="pwd">Opis:</label>
                <div class="col-sm-6">
                    {{form_widget(form.description)}}
                </div>
            </div>

            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-6">
                    {{form_widget(form.new,  {'attr': {'class': 'btn btn-info'}}) }}
                </div>
            </div> 
            {{form_end(form)}}
        {%endif%}
    </div>

{% endblock %}
